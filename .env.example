# MongoDB Connection
# Get your MongoDB URI from MongoDB Atlas or your MongoDB provider
# For production (Vercel), set this in Vercel environment variables dashboard
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/Jewellery_catalog?retryWrites=true&w=majority

# MongoDB Database Name (optional, will be extracted from URI if not provided)
MONGODB_DB=Jewellery_catalog

# Node Environment
NODE_ENV=development

# Frontend Environment Variables (must be prefixed with VITE_)
# API base URL for frontend (use /api for production)
VITE_API_BASE_URL=/api

# Scraper Configuration (hourly cache in milliseconds)
SCRAPE_CACHE_TTL=3600000

# Session Configuration (if using authentication)
SESSION_SECRET=your-secret-key-here-change-in-production
